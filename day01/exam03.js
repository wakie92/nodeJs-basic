/*
    기본 내장 모듈
    os
    url
    querystring
    util
    crypto
    filesystem
*/

var os = require('os');
console.log(os.hostname());
console.log(os.platform());
console.log(os.type());
console.log(os  );